export class Text {
  color: string;
  font: string;
  content: string;

  constructor(color: string, font: string, content: string) {
    this.color = color;
    this.font = font;
    this.content = content;
  }
}
